## SSAFY - 2022/08/03 내용 기록

---

<br />

#### SWEA_2805_농작물 수확하기 - 교수님 풀이

```java
package com.ssafy.day0803.swea;

import java.io.BufferedReader;
import java.io.IOException;
import java.io.InputStreamReader;
import java.util.Scanner;

public class SWEA_2805_농작물수확하기_교수님 {
	public static void main(String args[]) throws IOException	{

		Scanner sc = new Scanner(System.in);
		int T = sc.nextInt();

		for(int test_case = 1; test_case <= T; test_case++)
		{
			int ans = 0;
			int N = sc.nextInt();
			int[][] farm = new int[N][N];
			for(int i = 0; i < N; i++) {
				char[] data = sc.next().toCharArray();
				for(int j = 0; j < N; j++) {
					farm[i][j] = data[j] - '0';
				}
			}

			int center = N / 2;
			for(int i = 0; i < N; i++) {
				for(int j = 0; j < N; j++) {
					if(Math.abs(center - i) + Math.abs(center - j) <= center) {
						ans += farm[i][j];
					}
				}
			}
			
			System.out.println("#"+test_case+" "+ans);
		}
	}
}

```

<br />

#### SWEA_1873_상호의배틀필드 - 교수님 풀이
- 교수님 풀이에서 방향의 값을 String으로 

```java
tankDir = "^v<>".indexOf(map[x][y]) 
```
- 저렇게 string으로 방향을 설정하는 것도 좋은 방법인 것 같다.
- 시뮬레이션 구현의 경우에는 함수로 기능을 분리하는 것을 추천하고, 나중에 디버깅 및 유지보수 그리고 수정하기 편리하다.
- 기능분리를 하는 습관을 들이는 것이 좋다.(나중을 위해서라도)

<br />

---